# Pinned at scaffold time (commit 79d6c8d); no known compatibility
# constraint beyond "this is the version the project was built against."
requests==2.32.5
# Pinned at scaffold time (commit 79d6c8d); no known compatibility
# constraint beyond "this is the version the project was built against."
pytest==8.3.3
# Pinned when the bot skeleton was added (commit 338f167); no known
# compatibility constraint beyond "this is the version the project was
# built against."
discord.py==2.7.1
# Pinned when the Chroma index was added (commit 68923ea); no known
# compatibility constraint beyond "this is the version the project was
# built against."
chromadb==1.5.9
# An earlier pin (6.0.1) didn't exist on PyPI at all (latest was 5.1.2 at
# the time), so deps silently never installed and RAG/bot test modules
# silently failed to collect -- fixed in commit e06e6ae. Pinned to the
# confirmed-real, confirmed-installable version.
sentence-transformers==5.1.2
# Pinned below the version sentence-transformers/pip would otherwise resolve
# (2.14.0), which crashes on import: transformers' flex_attention integration
# triggers torch._dynamo init, and that torch release has a bug there --
# "ValueError: Duplicate dispatch rule for <built-in function intern>:
# already registered in VariableBuilder's id dispatch map". 2.6.0 confirmed
# import-safe with the same transformers/sentence-transformers versions above.
torch==2.6.0
# Added for hybrid BM25+vector retrieval (rag/bm25.py) -- confirmed
# importable and installable at this exact version.
rank_bm25==0.2.2
# Added for the CI lint step (ruff check .); pinned to the confirmed-real,
# confirmed-installable version used to establish the pyproject.toml
# ruleset and fix the resulting violations.
ruff==0.16.8
# Added for CI test-coverage reporting (pytest -q --cov); pinned to the
# confirmed-real, confirmed-installable version.
pytest-cov==7.1.0
