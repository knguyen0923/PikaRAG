requests==2.32.5
pytest==8.3.3
discord.py==2.7.1
chromadb==1.5.9
sentence-transformers==5.1.2
# Pinned below the version sentence-transformers/pip would otherwise resolve
# (2.14.0), which crashes on import: transformers' flex_attention integration
# triggers torch._dynamo init, and that torch release has a bug there --
# "ValueError: Duplicate dispatch rule for <built-in function intern>:
# already registered in VariableBuilder's id dispatch map". 2.6.0 confirmed
# import-safe with the same transformers/sentence-transformers versions above.
torch==2.6.0
anthropic==0.125.0
